#include<bits/stdc++.h>
using namespace std;

int main() {
    // int m = 24, n = 17;
    // int minN = min(m, n);
    // int maxN = max(m, n);
    // int i;
    // for(i = minN; i >= 1; i--) {
    //     if(maxN % i == 0 && minN % i == 0) break;
    // }
    // if(i == 1) cout << "true";
    // else cout << "false";

    // for(int d = 1; d < 60; d++) {
    //     if((37 * d) % 60 == 1) {
    //         cout << d;
    //         // break;
    //     }
    // }
    double x = pow(8, 7);
    double tmp = (int)x % 55;
    cout << tmp;
    return 0;
}